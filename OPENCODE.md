# OpenCode entrypoint

Read and follow `AGENTS.md` completely, then read the top of `tasks.md` and `README.md`.
Use the verified status and implementation order there. Do not treat planner previews
as motions or train on a task before its physical teacher and negative controls pass.
